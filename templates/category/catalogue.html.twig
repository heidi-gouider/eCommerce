{% extends 'base.html.twig' %}

{% block title %}Catalogue
{% endblock %}

{% block body %}
	{# /* <style>
							.example-wrapper {
								margin: 1em auto;
								max-width: 800px;
								width: 95%;
								font: 18px / 1.5 sans-serif;
							}
							.example-wrapper code {
								background: #F5F5F5;
								padding: 2px 6px;
							}
						</style> */ #}

	<div class="jumbotron txt center">
		<h1>CATALOGUE</h1>
	</div>
	{# <div class="example-wrapper"> #}
	<div class="row">
		{% for category in categories %}
			{# <div class="row justify-content-md-center"> #}
			<div class="col-4 mt-4">
				<div class="border border-dark rounded p-2 clearfix">
					<a href="/catalogue/{{ category.id }}">
						<h1>{{category.name}}</h1>
						<img src="{{category.image}}" class="img-fluid" height="150px" weight="150px">
						<h2>{{category.description}}</h2>

					</a>
				</div>
			</div>

		{% endfor %}
	</div>
{% endblock %}
